# Dependencias para la app (Docker / Streamlit)
-r base.txt

# UI
streamlit>=1.40.0

# Agentes y orquestación
langgraph>=0.2.0,<0.4.0
langchain>=0.3.0,<0.4.0
langchain-aws>=0.2.0,<0.3.0
langchain-core>=0.3.0,<0.4.0
langgraph-checkpoint-sqlite>=2.0.0,<3.0.0
chromadb==1.5.1

# Classifier runtime (deserializar modelo + matrices TF-IDF + SHAP)
joblib>=1.4.0
scipy>=1.13.0
scikit-learn>=1.5.0
xgboost>=3.0.0
matplotlib==3.9.2
shap==0.47.0
mlflow==2.17.2

# LLM providers
langchain-ollama>=0.3.0,<0.4.0
sentence-transformers==3.3.1

# Evaluación RAG (requerido por eval.yml — se ejecuta dentro del Docker)
ragas==0.2.15

# Observabilidad
langfuse>=2.7.3,<3.0.0
